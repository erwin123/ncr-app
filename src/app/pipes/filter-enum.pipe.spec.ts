import { FilterEnumPipe } from './filter-enum.pipe';

describe('FilterEnumPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterEnumPipe();
    expect(pipe).toBeTruthy();
  });
});
